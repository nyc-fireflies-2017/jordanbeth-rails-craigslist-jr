<h1>Craig's List Jr.</h1>

<h3>Categories</h3>
<% if @categories %>
  <% @categories.each do |category| %>
    <li><%=link_to("#{category.name}", category_articles_path(category.id))%></li>
  <% end %>
<% end %>

<%=link_to("New Category", new_category_path)%>
